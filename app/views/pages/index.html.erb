<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Find your wedding photographer!</title>
  </head>
  <body>                  
    <div class="container">
      <div class="main">
      
      <div class="filter_container">
      <div class="filter">FindYourPhotog</div>&nbsp;&nbsp;&nbsp;&nbsp;<div class="filter">500px</div>
      <!-- TODO javascript on and off switch here -->
      </div>
      
      <ul id="og-grid" class="og-grid">
        <% @photos.each do |photo| %>
          <li><%= link_to(image_tag(photo.photo.url(:thumb)), photo) %></li>
        <% end %>
      </ul>
      
      
      <ul id="og-grid" class="og-grid">
          <% @f00_photos.each do |photo|%>
          <li><%= link_to(image_tag(photo[0..-6]+'3.jpg'), root_path )%></li>  
          <% end %>
      </ul>
      
    </div>
    </div>
  </body>
</html>
        
        